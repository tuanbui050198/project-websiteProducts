const styles = () => ({
  loading: {
    position: 'fixed',
    left: 0,
    right: 0,
    top: 0,
    bottom: 0,
    zIndex: 10,
    background: "#000",
    opacity: 0.4,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  icon: {
    position: 'fixed',
    marginLeft: 'auto',
    marginRight: 'auto',
    width: 100,
  },
});

export default styles;
